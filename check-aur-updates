#!/bin/bash

set -e

ls -1d /var/abs/aur/* | while read d; do
    pushd "$d" > /dev/null
    git fetch
    if [ 0 -ne "$(git diff-index --cached origin/master | wc -l)" ]; then
        echo "$d"
    fi
    popd > /dev/null
done
